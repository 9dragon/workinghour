# ==================== 环境配置示例文件 ====================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 修改以下配置项
# 3. 确保 .env 文件不要提交到 Git 仓库

# ==================== 安全配置（必须修改）====================

# JWT 密钥 - 用于生成和验证 JWT 令牌
# 请使用强随机字符串，至少 32 个字符
# 生成命令：openssl rand -hex 32
JWT_SECRET_KEY=change-this-to-a-strong-random-jwt-secret-key-min-32-chars

# Flask 密钥 - 用于 session 和 CSRF 保护
# 请使用强随机字符串，至少 32 个字符
FLASK_SECRET_KEY=change-this-to-a-strong-random-flask-secret-key

# ==================== 数据库配置 ====================

# SQLite 数据库文件路径（使用绝对路径）
# Linux: /var/www/workinghour/data/workinghour.db
# Windows: C:\inetpub\workinghour\data\workinghour.db
DATABASE_PATH=/var/www/workinghour/data/workinghour.db

# ==================== CORS 配置 ====================

# 允许的前端域名（逗号分隔）
# 生产环境示例：https://yourdomain.com,https://www.yourdomain.com
# 开发环境示例：http://localhost:8080,http://127.0.0.1:8080
ALLOWED_ORIGINS=http://localhost:8080,https://yourdomain.com

# ==================== 服务器配置 ====================

# 服务器主机地址（0.0.0.0 表示监听所有网卡）
HOST=0.0.0.0

# 服务器端口
PORT=8000

# 调试模式（生产环境必须设置为 False）
DEBUG=False

# 测试模式（生产环境必须设置为 False）
TESTING=False

# ==================== 日志配置 ====================

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径（可选，默认使用控制台输出）
# LOG_FILE=/var/log/workinghour/app.log

# ==================== 文件上传配置 ====================

# 最大文件上传大小（MB）
MAX_CONTENT_LENGTH=10

# 允许的文件扩展名（逗号分隔）
ALLOWED_EXTENSIONS=xls,xlsx

# ==================== 系统默认配置 ====================

# 文件保留周期（天）
FILE_RETENTION_DAYS=7

# 单次最大导入行数
MAX_IMPORT_ROWS=1000

# 最大文件大小（MB）
MAX_FILE_SIZE=10

# ==================== JWT 配置 ====================

# JWT 算法（推荐使用 HS256）
JWT_ALGORITHM=HS256

# Token 过期时间（小时）
JWT_EXPIRATION_HOURS=24

# ==================== 密码策略 ====================

# 密码最小长度
PASSWORD_MIN_LENGTH=6

# 账号锁定阈值（连续失败次数）
LOGIN_FAIL_THRESHOLD=5

# 账号锁定时间（分钟）
ACCOUNT_LOCK_MINUTES=30

# ==================== 备份配置 ====================

# 备份文件保留数量
BACKUP_RETENTION_COUNT=10

# 自动备份间隔（小时，0 表示禁用自动备份）
AUTO_BACKUP_INTERVAL=0

# ==================== 生产环境额外建议 ====================

# 1. 使用环境变量管理敏感信息（推荐使用专业的密钥管理服务）
# 2. 配置 HTTPS（使用 Let's Encrypt 或购买 SSL 证书）
# 3. 设置防火墙规则，仅开放必要端口（80, 443, 22）
# 4. 定期更新系统和依赖包
# 5. 配置日志监控和告警
# 6. 实施定期的数据库备份策略
# 7. 使用进程管理工具（systemd, supervisord）确保服务稳定运行
# 8. 考虑使用反向代理（Nginx, Apache）处理静态文件和 SSL
